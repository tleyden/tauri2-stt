[package]
name = "whisperkit-swift-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
swift-rs = "1.0.6"

[build-dependencies]
swift-rs = { version = "1.0.6", features = ["build"] }
